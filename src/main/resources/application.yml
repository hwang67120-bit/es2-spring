spring:
  application:
    name: ec2-spring
    # CI/CD 테스트 - 2026.05.20

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: info,health
  info:
    env:
      enabled: true

info:
  team-name: ${team-name}

logging:
  level:
    root: INFO
